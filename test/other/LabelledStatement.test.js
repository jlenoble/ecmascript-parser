import {makeTest} from './helpers';

makeTest({
  grammar: 'LabelledStatement',
  dir: 'lexical-grammar',
  files: [
    'LabelledStatement1',
    'LabelledStatement2',
    'LabelledStatement3',
    'LabelledStatement4',
    'LabelledStatement5',
  ],
});
